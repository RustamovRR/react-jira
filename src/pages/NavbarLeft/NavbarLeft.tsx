import React from 'react';

import { Icon, AboutTooltip } from 'components/shared';

import {NavLeft, LogoLink, StyledLogo, Bottom, Item, ItemText} from './NavbarLeftStyles';

export type Props = {};

const NavbarLeft = (props:Props) => {
    return (
        <div>
            NavbarLeft
        </div>
    );
}

export default NavbarLeft;